<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceApp"
        tools:targetApi="31">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".presentation.ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AttendanceApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".presentation.ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AttendanceApp.NoActionBar" />

        <!-- Student Activities -->
        <activity
            android:name=".presentation.ui.student.StudentMainActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.scanner.QRScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".presentation.ui.checkin.CheckInActivity"
            android:exported="false"
            tools:ignore="MissingClass" />

        <activity
            android:name=".presentation.ui.history.AttendanceHistoryActivity"
            android:exported="false" />

        <!-- Teacher Activities -->
        <activity
            android:name=".presentation.ui.teacher.TeacherMainActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.teacher.CreateClassActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.teacher.CreateSessionActivity"
            android:exported="false" />

        <!-- GPS Test Activity -->
        <activity
            android:name=".presentation.ui.gps.GPSTestActivity"
            android:exported="false" />

        <!-- View QR Code Activity -->
        <activity
            android:name=".presentation.ui.viewQR.ViewQRCodeActivity"
            android:exported="false" />

        <!-- ZXing Capture Activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            tools:replace="screenOrientation" />

    </application>

</manifest>
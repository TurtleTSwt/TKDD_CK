<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceApp"
        tools:targetApi="31">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".presentation.ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AttendanceApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".presentation.ui.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AttendanceApp.NoActionBar" />

        <!-- Student Activities -->
        <activity
            android:name=".presentation.ui.StudentMainActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.QRScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".presentation.ui.CheckInActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.AttendanceHistoryActivity"
            android:exported="false" />

        <!-- Teacher Activities -->
        <activity
            android:name=".presentation.ui.TeacherMainActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.CreateClassActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.CreateSessionActivity"
            android:exported="false" />

        <!-- ZXing Capture Activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

    </application>

</manifest>